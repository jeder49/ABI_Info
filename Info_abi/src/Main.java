public class Main {
    public static void main(String[] args) {
        //create object

        //Geheimnis prinzip & extend (dt.: vererben)

        //abstract

        //Backtracking

        // dynamic list (stack & queue) & sorting algorithms & search algorithms
        List l = new List();
        l.addToList0(new Node(null,null));

        //Tree & recursion &
        /*
        Tree t = new Tree(new TreeNode("hi",null,null));
        t.addNode(new TreeNode("ab",null,null));
        t.addNode(new TreeNode("an",null,null));
        t.addNode(new TreeNode("top",null,null));
        t.addNode(new TreeNode("stop",null,null));
        System.out.println(t.inorder(t.getRoot(),""));
         */

        //graphical recursion

    }
}